# Generated Script Output

This folder contains generated prompts, voiceover drafts, and small state files
used by the social/video helper scripts in `scripts/`.

The executable script files stay one level up in `scripts/`; generated run
outputs live here so the script directory remains readable.

New generated drafts are ignored by default. Commit a specific output only when
it is intentionally promoted into project history.
